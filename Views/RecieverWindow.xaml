<Window x:Class="LR_1.Views.RecieverWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LR_1.ViewModels"
        mc:Ignorable="d"
        Title="RecieverWindow" Height="360" Width="355">

    <Grid>
        <StackPanel>
            <Label Content="Полученное сообщение" Height="30" FontSize="14"/>
            <TextBox x:Name="RecievedMessage" Text="{Binding RecievedMessage}" TextWrapping="Wrap" Height="90" FontSize="14"/>
            <Button Command="{Binding DecodeTextCommand}" Content="Декодировать"  Height="30" FontSize="14"/>
            <Label Content="Декодированное сообщение" Height="30" FontSize="14" />
            <TextBox x:Name="DecodedText" Text="{Binding DecodedText}" TextWrapping="Wrap" Height="90" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>
